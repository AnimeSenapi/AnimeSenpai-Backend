# Suppress audit warnings for dev dependencies that don't affect runtime security
# @vercel/node has outdated transitive dependencies (esbuild, undici, path-to-regexp)
# but these are only used during build time on Vercel's servers, not in production runtime
audit-level=high

